<h1>All Cats</h1>
<a href="<%= new_session_url %>">Log In</a>

<ul>
  <% @cats.each do |cat| %>
  <li><%= link_to cat.name, cat_url(cat.id) %></li>
<% end %>
</ul>

<%= link_to "Create a new cat", new_cat_url %>
<br>
<%= link_to "Submit rental request", new_cat_rental_request_url %>
